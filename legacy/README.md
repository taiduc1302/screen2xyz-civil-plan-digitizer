# Legacy prototypes

The M1 review lab and the original synthetic OCR lab are retained here for historical reference. They are not part of Screen2XYZ v2, are not imported by the application, and their old test suites are retired from CI.

The M2 package remains active because Screen2XYZ v2 reuses its parsing, stability, scheduling, journal, state-machine, profile, and Windows OCR foundations.
