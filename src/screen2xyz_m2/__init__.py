"""Screen2XYZ M2-Live — opt-in live screen-region watcher.

Explicit target selection -> countdown snapshot region setup -> mandatory
synchronized preview -> ARM -> explicit Start -> ~1 Hz synchronized
capture/OCR -> live grid + retained-event journal -> approved exports.

Source-agnostic, local-only, no network, no hidden capture, no pointer
control. Synthetic evidence only until G-E-REAL passes; conceptual and
preliminary estimating data only.
"""

__version__ = "0.1.0"
