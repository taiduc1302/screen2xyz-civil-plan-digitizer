# Source

This folder contains only the G4-authorized synthetic OCR laboratory package in `screen2xyz_lab/`. It is experimental current-machine lab code, not an accepted production application. It uses locally generated PNG inputs and does not implement live capture, cursor control, coordinate transformation, a named-service preset, or downstream import.

Conceptual and preliminary estimating data only.
