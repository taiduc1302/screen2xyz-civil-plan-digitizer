# Optional dependency for vector PDF page/text inspection.
# The guaranteed manual PNG workflow remains standard-library only.
pypdf==6.14.2

# Estimator-facing XLSX creation and generated-workbook round-trip validation.
openpyxl==3.1.5

# XML hardening used by openpyxl when reading generated or supplied workbooks.
defusedxml==0.7.1
