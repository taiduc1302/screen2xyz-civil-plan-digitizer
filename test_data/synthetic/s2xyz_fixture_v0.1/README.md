# Synthetic Fixture v0.1

This registered fixture contains exactly 60 deterministic locally generated scenarios (`S001`–`S060`), three text-size conditions, evaluator-only ground truth and render jobs, a separated runtime manifest, and 60 generated PNGs. The seed is `20260715`; the horizontal CRS label is `EPSG:4326`, elevation unit is metres, and the synthetic vertical reference is `SYNTHETIC_LOCAL`.

`fixture_manifest.json` contains no visible text, expected value, expected disposition, expected code, or reference relationship. Runtime OCR reads only its bounded image entries. Generated screenshots are not live screen capture.

Conceptual and preliminary estimating data only.
