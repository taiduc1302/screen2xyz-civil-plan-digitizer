# Gate G4 - Scoped Implementation Authorization

| Field | Decision |
|---|---|
| Task | S2XYZ-CODEX-003 |
| Prerequisites | G1, G2, and G3 conditionally passed and recorded |
| Result | Passed - conditional and run-scoped |
| Authorization | Conditionally approved by the project owner through S2XYZ-CODEX-003 for the scoped synthetic OCR lab only. |
| Output classification | Conceptual and preliminary estimating data only. |

## Evidence

- Architecture Specification v0.2 and Architecture Decision Record v0.2 define the smallest observed current-machine stack, replaceable interfaces, separation, failure states, atomic publication, and reversibility.
- Test Plan, 41-row Test Matrix, 15 Acceptance Criteria, and exact 60-scenario Fixture Specification exist and are mutually traceable to 40 mandatory requirements.
- Retained generated-image OCR preflight and selected-host recheck prove actual local pixel OCR, bounded failure status, timeout enforcement, sanitized environment, font identity, and zero-package isolation.
- Dataset and Dependency/Licence registers are complete enough for private current-machine testing without claiming legal approval, redistribution, a repository licence, or public release.
- Final read-only Architecture/Dependency, QA/Test Design, and Data/Dependency reviews report zero unresolved Blocker or Major finding.
- Parent mapping, hash, dependency, and `git diff --check` validations pass.

## Parent decision

All G4 prerequisites in the controlling task are satisfied. Implementation is authorized only for the documented current-machine, locally generated, synthetic-image OCR laboratory. It may implement fixture generation, generated PNG rendering, actual local OCR, raw evidence, parsing, validation, stale/duplicate classification, deterministic source-coordinate demonstration export, metrics, tests, and retained evidence.

This decision does not authorize real data, a named-service preset, cursor or GUI automation, live capture claims, coordinate transformation, downstream automation/import claims, terrain/earthwork calculation, production architecture, a licence, public release, or merge of the branch or pull request.
