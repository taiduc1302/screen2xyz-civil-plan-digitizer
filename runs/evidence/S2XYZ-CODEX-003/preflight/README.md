# S2XYZ-CODEX-003 Preflight Evidence

Conceptual and preliminary estimating data only.

This directory retains sanitized, current-machine evidence used for the G4 architecture decision. The OCR probe image is locally generated synthetic text. It is an environment diagnostic, not application implementation, live screen capture, real-source data, production validation, or downstream compatibility evidence.

No username, hostname, absolute personal path, environment-variable dump, credential, token, network address, or unrelated package inventory is retained here.

The initial long-lived PowerShell host reported version 5.1.26100.8655. A pre-G4 reconciliation found the active executable and new child-process host at 5.1.26100.8875, reran actual OCR successfully, and retained the correction in `environment_recheck.txt` and `ocr_probe_recheck_observation.json`. The implementation architecture uses the rechecked child-process version; the earlier observation is preserved rather than silently rewritten.
