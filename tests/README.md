# Tests

This folder contains the 42-test automated catalogue for the G4-authorized synthetic OCR laboratory. `run_all.py` records exact test IDs into a fresh staged result under `.lab_work`; the separate `finalize-evidence` command publishes that passing result and writes the retained evidence manifest last. Test results describe only locally generated images on the current Windows computer.

Conceptual and preliminary estimating data only.
